# codex-testing-1
Private for testing with OpenAI CODEX
Code is for a browser-based video game.


## Run
- Use `index.html` as the app entry point (some online compilers/hosts require this exact filename).
- `index.html` loads `styles.css` and `main.js` from the same directory.
